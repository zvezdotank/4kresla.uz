The site's only form. Use inside `Dialog` or on a light section.

```jsx
<BookingForm onSubmit={() => close()} />
```

Field order is fixed: name, phone, service, master, date. Never ask for more than these.
