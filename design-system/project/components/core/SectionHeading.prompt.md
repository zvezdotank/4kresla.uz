Section title — uppercase, wide-tracked, centered by default.

```jsx
<SectionHeading title="Наша команда" />
<SectionHeading title="Прайс-лист" rule subtitle="Цены указаны за один визит." />
```

Use `tone="light"` on dark photo bands. Titles are 1–3 words, no punctuation.
